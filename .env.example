# Database (remove these 5 lines if you don't use a DB)
DB_HOST=your-db-host
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your_database_name
DB_PORT=3306

# Add any other API keys your tools need below

# Code participants must enter before register_user will accept them
QUIZ_ACCESS_CODE=your-quiz-access-code
